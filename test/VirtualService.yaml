apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: springboot-vs
  namespace: springboot
spec:
  hosts:
  - "*"
  gateways:
  - springboot-gateway
  http:
  - match:
    - uri:
        prefix: /home
    route:
    - destination:
        host: kubernetes.default.svc
        port:
          number: 6789